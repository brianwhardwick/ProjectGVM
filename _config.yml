# -----------------------------
# Jekyll configuration
# -----------------------------

plugins:
  - jekyll-sitemap

# Exclude all markdown and html files by default
exclude:
  - "**/*.md"
  - "**/*.html"

# Explicitly include ONLY index.html files
include:
  - index.html

# Ensure clean, directory-style URLs
permalink: pretty
